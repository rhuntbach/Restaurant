namespace Restaurant
{
    internal class Program
    {
        // Declare a MEMBER here to keep track of order number. This is a class level MEMBER,
        // so it will need an access modifier (private/public/etc...)

        static void Main(string[] args)
        {
            // Create a Dispatcher here and initialize it with 3 Cook class objects

            // Write to console and prompt the server to enter their order

            // Read the order text from the console

            // Create an Order object and initialize it with the order text and order number

            // Call the dispatcher and send it the Order object. Output, something like "Order
            // 123 is up!" when the dispatcher returns.

            // Increment the order number
        }
    }
}